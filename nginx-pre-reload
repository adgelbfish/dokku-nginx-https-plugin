#!/usr/bin/env bash
# Runs a check against all nginx conf files
# to ensure they are valid

echo "setting keycloak https"
sed -i 's=http://keycloak=https://keycloak=g' /home/dokku/keycloak/nginx.conf

